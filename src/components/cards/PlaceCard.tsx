import { memo } from 'react';import { Image } from 'expo-image';import { StyleSheet, Text, View } from 'react-native';import type { Place } from '../../types/travel';import { colors, radius, shadows, spacing, typography } from '../../theme';import { Icon } from '../ui/Icon';import { AnimatedPressable } from '../ui/AnimatedPressable';
export const PlaceCard=memo(function PlaceCard({place}:{place:Place}){return <AnimatedPressable style={s.card}><Image source={{uri:place.image}} style={s.image} contentFit="cover" transition={300}/><View style={s.heart}><Icon name="heart" color={colors.white} size={20}/></View><View style={s.copy}><Text numberOfLines={1} style={s.name}>{place.name}</Text><Text style={s.country}>{place.country}</Text><Text style={s.rating}><Text style={s.star}>★ </Text>{place.rating}</Text></View></AnimatedPressable>});const s=StyleSheet.create({card:{width:164,height:226,borderRadius:radius.md,backgroundColor:colors.surface,overflow:'hidden',...shadows.soft},image:{height:137,width:'100%'},heart:{position:'absolute',right:spacing.xs,top:spacing.xs,width:30,height:30,borderRadius:radius.pill,backgroundColor:colors.softOverlay,alignItems:'center',justifyContent:'center'},copy:{padding:spacing.sm},name:{...typography.label,color:colors.ink},country:{...typography.caption,color:colors.muted,marginTop:spacing.xxs},rating:{...typography.caption,color:colors.ink,marginTop:spacing.xs},star:{color:colors.gold}});
