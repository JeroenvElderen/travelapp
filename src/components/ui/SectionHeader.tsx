import { StyleSheet, Text, View } from 'react-native'; import { colors, spacing, typography } from '../../theme'; import { AnimatedPressable } from './AnimatedPressable'; import { Icon } from './Icon';
export function SectionHeader({title,onPress}:{title:string;onPress?:()=>void}){return <View style={s.row}><Text style={s.title}>{title}</Text><AnimatedPressable style={s.action} onPress={onPress}><Text style={s.view}>View all</Text><Icon name="arrow" size={20}/></AnimatedPressable></View>};const s=StyleSheet.create({row:{flexDirection:'row',alignItems:'center',justifyContent:'space-between',marginBottom:spacing.md},title:{...typography.title,color:colors.ink},action:{flexDirection:'row',alignItems:'center',gap:spacing.xs},view:{...typography.label,color:colors.ink}});
