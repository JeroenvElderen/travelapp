import { memo } from 'react'; import { StyleSheet, Text, View } from 'react-native'; import type { Category } from '@/types/travel'; import { colors, radius, shadows, spacing, typography } from '@/lib/theme'; import { Icon } from '@/components/ui/Icon'; import { AnimatedPressable } from '@/components/ui/AnimatedPressable';
export const CategoryBar=memo(function CategoryBar({items,active,onChange}:{items:Category[];active:string;onChange:(id:string)=>void}){return <View style={s.wrap}>{items.map(x=>{const selected=x.id===active;return <AnimatedPressable key={x.id} onPress={()=>onChange(x.id)} style={[s.item,selected&&s.selected]}><Icon name={x.icon} size={26} color={selected?colors.white:colors.ink}/><Text style={[s.label,selected&&s.selectedLabel]}>{x.label}</Text></AnimatedPressable>})}</View>});
const s=StyleSheet.create({wrap:{marginTop:-28,marginHorizontal:spacing.md,padding:spacing.sm,borderRadius:radius.xl,backgroundColor:colors.surface,flexDirection:'row',justifyContent:'space-between',...shadows.soft},item:{width:52,height:72,borderRadius:radius.lg,alignItems:'center',justifyContent:'center',gap:spacing.xs},selected:{backgroundColor:colors.forest,...shadows.soft},label:{...typography.caption,color:colors.ink},selectedLabel:{color:colors.white,fontWeight:'700'}});
